import Foundation

// The generated types (Proof, CredentialType, Session, etc.) are automatically
// available since they're in the same module (IDKit). No need to re-export.

/// Main entry point for IDKit Swift SDK
public enum IDKit {
    /// Version of the IDKit SDK
    public static let version = "3.0.0"
}
